[project]
name = "seal-semantic"
version = "0.1.0"
description = "YAML-based semantic metrics layer for Seal"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.13.5",
    "pyyaml>=6.0.3",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1",
]
